<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-07-05T03:12:54Z

The restarted stack was stopped again, so I'll take the hint and stop relaunching it — it looks like the process is being managed from your side now.

State of the world: nothing is running on :3000/:3001, the database container is still up, and every change is committed and pushed. When you want the app up, run `pnpm dev` from the repo root (API :3000, web :3001, open http://localhost:3001) — then the Connect Google button on /connections is ready to test with the corrected flow.
