<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-07-05T01:11:34Z

Root cause found for the 27-vs-23 discrepancy, and it's two real (small) defects:

1. **Charts overcount**: the OVERDUE bucket counts every past-due AUTHORISED invoice even when `AmountDue` is 0 (fully credited or paid down to zero) — so the dashboard says "27 overdue" while only 23 actually owe money. The bucket should skip zero-balance invoices.
2. **Chat tool undercounts at scale**: `list_unpaid_invoices` computes its outstanding/overdue totals *after* slicing to 50 rows, so with more than 50 unpaid invoices the totals would silently shrink. Not binding today (~38 unpaid) but a lurking demo-day trap.

I'm holding both fixes until the QA sweep finishes — the API restarts on file save in watch mode, and I don't want to yank the server out from under the browser mid-sweep. They go in with whatever else QA finds, then the final commit.
