<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-07-05T09:07:56Z

Take a breath — **the email fetched, and scenario B is fully done.** It completed at 10:02 your time, probably moments after you messaged me. The full autonomous chain is on the audit trail:

1. Gmail poll read 1 message from devansh8801@gmail.com (the only sender it's allowed to read)
2. `email.agreement_detected` — the PDF recognised as a signed agreement
3. **Xero contact created** for Northbeam-DKaria (`272f780a...`)
4. Potential client **promoted to a real client** (that's why the watched queue looks empty — he graduated)
5. Contract parsed and filed: **12 clauses plus the structured billing profile** (£50 base, £40 beyond 3h/week, 30-minute minimum block, cited to Clause 2.1)
6. The "File a contract" task auto-resolved — zero clicks, zero open tasks

So both live scenarios have now run end to end: CivicPulse's zero-click invoice (INV-0073 authorised in Xero) and Northbeam's email auto-onboard.

**Why it looked stuck — a real bug, now fixed and pushed (`5ec3c44`).** The old 30-minute IMAP/fixture email cron was still running alongside the Gmail sync, and it did two bad things. Cosmetically, it flipped the EMAIL pill on Connections to FALLBACK twice an hour, which is probably what you saw. Dangerously, after reading only fixture files it still advanced each watched sender's "last polled" watermark — and the Gmail query only looks *after* that watermark, so a real email arriving in the wrong two-minute window would be skipped forever. Your email likely fell into exactly that hole at 10:00 before the 10:02 sync caught it. The fixture poller now stands down completely whenever Google holds the gmail scope: no status flapping, no watermark clobbering. Verified live — the poll endpoint now answers "Gmail owns the email surface" and the EMAIL pill stays LIVE.

Remaining loose end from earlier: INV-0073's email to kariadevansh08 still hasn't gone out — Xero's Demo Company keeps 500ing the email endpoint with everything valid on our side. Robyn retries it on each sync cycle (throttled to every 45 minutes) and audits each attempt; if it never lands, one click on the invoice in the Xero UI sends it, and the honest `email_failed` audit row is a good production-readiness beat on stage regardless.
