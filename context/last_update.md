<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-07-04T22:38:39Z

Robyn is fully up and live. Everything the handoff promised checks out on your machine:

- **Web**: http://localhost:3000 — serving (open it there)
- **API**: http://localhost:4000/api (Swagger at http://localhost:4000/api/docs), health OK
- **Postgres**: `robyn-db` container, healthy (on port 5433 locally since your MyMan project owns 5432)
- **Xero**: `G0 PASS` — token OK, tenant **Demo Company (UK)**, 114 invoices visible; the Connections panel shows one LIVE connection (Xero) and two honest FALLBACKs (calendar via seeded `.ics`, email via fixture mailbox — by design until Google/IMAP creds are added)
- **Seed**: ran clean and proved idempotent — all 3 Xero contacts and 17 invoices were *reused*, not duplicated, and the local DB has the full story (3 clients, 6 meetings, 18 proposals, 3 detections)

The cold-open state matches the demo runbook exactly: **3 open tasks** (provide transcript, confirm agreement, review invoice) and the leak strip reads **June 2026 with £2,880 recoverable from ledger detections** (£3,555 total including the invoice in review and an unbilled meeting).

Two notes:

1. The demo runbook in `HANDOFF.md` is ready to walk — beat 2 (paste a transcript on the Fenwick card → approve → live AUTHORISED invoice in Xero with history note + attachments) is the money moment; worth one rehearsal run tonight for timing.
2. Small doc discrepancy I noticed: HANDOFF claims the app demos without Xero creds, but `env.ts` hard-requires them at boot. Moot now that you're live, but worth knowing if creds ever get rotated mid-demo — the API won't boot half-way, it just stops with a named error.

The dev servers are running in the background; say the word if you want them stopped, or `make stop` kills the database when you're done for the night.
