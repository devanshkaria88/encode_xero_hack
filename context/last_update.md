<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-07-04T22:00:00Z

Full end-to-end Robyn build COMPLETE (unattended). Backend (3 loops + engine + Xero writes), seed, and all 5 frontend surfaces built, committed, QA'd. Chrome DevTools sweep green on every route (zero console errors, all network 200, clean at 1440/1280, light+dark). Demo beats 1-3 rehearsed LIVE with the real LLM (transcript→£810 invoice, email→client onboarded); screenshots in docs/qa/. One open item, owned by Devansh: fix the Xero Custom Connection creds and every write goes live (HANDOFF.md).

## Decisions

- **Cursor never started `web/`** — Claude built the whole stack (api + web + seed) to hit the end-to-end mandate.
- **Xero creds are a standard OAuth2 app, not a Custom Connection** (G0.5). Confirmed live + by a research pass: client_credentials filters out every accounting scope; `/connections` 400s. Chosen path: **live-only, no local ledger fallback** (per Devansh). Every Xero write is coded to the Custom Connection contract and catches+surfaces "Xero connection pending" until `verify:xero` passes. Fix steps in HANDOFF.md (fresh Custom Connection → Demo Company → paste ID/secret).
- **All Xero access via the raw Accounting API** through one cached token path (`api/src/modules/xero/`) with a broad→granular scope fallback + tenant resolution; chosen over the MCP server (per-call re-auth, no idempotency, can't flip status). MCP named on the architecture slide for sponsor visibility.
- **Product is ACCREC** (Robyn invoices clients). Skills were ACCPAY-first; mechanics reused with `Type: "ACCREC"`.
- **Decoupled module architecture** so 8 backend modules + 5 frontend surfaces built in parallel with zero cross-imports: each module owns its Tasks + @Cron + ConnectionState row; task resolution lives in the owning endpoint; shared invoice writes go through `XeroService.writeInvoice`. Contract pinned in `context/BUILD_SPEC.md`.
- **Build orchestration:** foundation + engine hand-built; feature modules, seed, and frontend surfaces built by parallel subagent workflows against the fixed kernel, then integration-compiled and live-smoke-tested. Calendar surface + two agents that stalled on injected noise were finished directly.
- **Stack:** NestJS + TypeORM (synchronize, no migrations) + Postgres via docker compose; Next.js 14 + Tailwind + shadcn; pnpm 9.15.4; LLM edges on `claude-sonnet-5` with zod validation.
- **Fallbacks:** IMAP empty → fixture mailbox; Google OAuth empty → `.ics`; both shown truthfully as FALLBACK on Connections.
- **Fixes found in QA:** transcript prompt tightened (only work beyond the meeting time is billable — clean £810 hero invoice); detector idempotency guard (no duplicate detections, £2,880 headline stable); api tsconfig stopped nesting `dist/main.js`; next-themes hydration-mismatch on the dark toggle resolved.
