{
  "openapi": "3.0.0",
  "paths": {},
  "info": {
    "title": "Robyn API",
    "description": "Robyn turns a freelancer's calendar, contracts and transcripts into invoices in Xero. Every agent behaviour is a state transition that writes to Xero or raises a Task. The LLM parses and proposes at the edges; it never decides and never sends.",
    "version": "0.1.0",
    "contact": {}
  },
  "tags": [
    {
      "name": "tasks",
      "description": "Tasks inbox — resolvable cards"
    },
    {
      "name": "meetings",
      "description": "Calendar loop — meetings, transcripts, matching"
    },
    {
      "name": "clients",
      "description": "Clients, contracts, autonomy, potential-client queue"
    },
    {
      "name": "proposals",
      "description": "Invoice proposals with line provenance"
    },
    {
      "name": "detections",
      "description": "Ledger detectors (loop 3)"
    },
    {
      "name": "connections",
      "description": "Integration health + check-now"
    },
    {
      "name": "dashboard",
      "description": "Aggregate feeds: calendar view, leak strip, audit"
    },
    {
      "name": "internal",
      "description": "Cron jobs exposed as POST /internal/run/:job"
    }
  ],
  "servers": [],
  "components": {
    "schemas": {}
  }
}